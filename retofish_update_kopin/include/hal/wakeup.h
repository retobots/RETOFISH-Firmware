// #ifndef WAKEUP_H
// #define WAKEUP_H

// #include <Arduino.h>

// // Lớp tiện ích để xử lý logic sleep & wake cho hệ thống
// class Wakeup {
// public:
//     // Tính số giây còn lại đến giờ cho ăn kế tiếp (dựa trên DS1307 + ScheduleManager)
//     static int calculateSecondsToNextFeeding();

//     // Kích hoạt deep sleep + cấu hình wakeup bằng timer + nút nhấn
//     static void goToDeepSleep();
// };

// #endif
